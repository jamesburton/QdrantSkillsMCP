---
phase: 01-core-mcp-server
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/QdrantSkillsMCP.UnitTests/Yaml/SkillParserTests.cs
  - tests/QdrantSkillsMCP.UnitTests/Validation/SkillNameValidatorTests.cs
  - tests/QdrantSkillsMCP.UnitTests/Session/InMemorySessionTrackerTests.cs
  - tests/QdrantSkillsMCP.UnitTests/Embedding/OpenAiEmbeddingServiceTests.cs
autonomous: true
requirements:
  - DIST-03

must_haves:
  truths:
    - "YAML frontmatter parses correctly and round-trips losslessly (including edge cases)"
    - "Skill name validation rejects invalid names and accepts valid ones"
    - "Session tracker correctly tracks loaded skills with thread safety"
    - "Embedding service delegates to IEmbeddingGenerator correctly"
  artifacts:
    - path: "tests/QdrantSkillsMCP.UnitTests/Yaml/SkillParserTests.cs"
      provides: "Parse + round-trip tests for YAML frontmatter"
      min_lines: 60
    - path: "tests/QdrantSkillsMCP.UnitTests/Validation/SkillNameValidatorTests.cs"
      provides: "Validation rule tests"
      min_lines: 40
    - path: "tests/QdrantSkillsMCP.UnitTests/Session/InMemorySessionTrackerTests.cs"
      provides: "Session tracking behavior tests"
      min_lines: 30
    - path: "tests/QdrantSkillsMCP.UnitTests/Embedding/OpenAiEmbeddingServiceTests.cs"
      provides: "Embedding service delegation tests"
      min_lines: 20
  key_links:
    - from: "tests/QdrantSkillsMCP.UnitTests"
      to: "src/QdrantSkillsMCP.Infrastructure"
      via: "ProjectReference"
      pattern: "ProjectReference.*Infrastructure"
---

<objective>
Create comprehensive unit tests for all infrastructure services that can be tested without external dependencies. These tests verify business logic in isolation.

Purpose: Unit tests catch regressions fast and validate the core logic (parsing, validation, session tracking) without needing Qdrant or OpenAI.
Output: Full unit test suite that passes with `dotnet test tests/QdrantSkillsMCP.UnitTests`.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-mcp-server/01-CONTEXT.md
@.planning/phases/01-core-mcp-server/01-RESEARCH.md
@.planning/phases/01-core-mcp-server/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for SkillParser and SkillNameValidator</name>
  <files>
    tests/QdrantSkillsMCP.UnitTests/Yaml/SkillParserTests.cs,
    tests/QdrantSkillsMCP.UnitTests/Validation/SkillNameValidatorTests.cs
  </files>
  <behavior>
    SkillParser tests:
    - Parse standard skill: frontmatter with name, description, tags + markdown body -> extracts all fields correctly
    - Round-trip lossless: parse then access rawContent -> identical to original input (byte-for-byte)
    - Missing frontmatter: plain markdown with no --- delimiters -> treated as body-only, no metadata
    - Empty body: frontmatter only with no markdown after closing --- -> body is empty string
    - Extra frontmatter fields: unknown YAML keys -> preserved (not dropped)
    - Multiline description: YAML multiline string in description -> parsed correctly
    - Special characters in content: backticks, pipes, brackets in markdown body -> not corrupted

    SkillNameValidator tests:
    - Valid: "my-skill", "a", "skill-123", "a-b-c" -> IsValid=true
    - Invalid empty: "" -> IsValid=false with error message
    - Invalid uppercase: "My-Skill" -> IsValid=false
    - Invalid special chars: "my_skill", "my.skill", "my skill" -> IsValid=false
    - Invalid start/end hyphen: "-skill", "skill-" -> IsValid=false
    - Invalid too long: 65+ chars -> IsValid=false
    - Valid max length: exactly 64 chars -> IsValid=true
    - Valid single char: "a" -> IsValid=true
  </behavior>
  <action>
    Create test classes using xunit.v3 `[Fact]` and `[Theory]` attributes. Use `[InlineData]` for parameterized validation tests.

    For SkillParser tests:
    - Create sample skill content strings as test data (realistic markdown with YAML frontmatter).
    - Test the Parse method returns correct metadata fields AND that rawContent is unchanged.
    - Use `Assert.Equal` with exact string comparison for round-trip verification.

    For SkillNameValidator tests:
    - Use `[Theory]` with `[InlineData]` for valid/invalid name cases.
    - Assert both `IsValid` boolean and error message content.
  </action>
  <verify>
    <automated>cd C:/Development/MCP/QdrantSkillsMCP &amp;&amp; dotnet test tests/QdrantSkillsMCP.UnitTests --filter "SkillParser|SkillNameValidator" -v normal</automated>
  </verify>
  <done>All parser tests pass including lossless round-trip. All validator tests pass with correct valid/invalid classification. Edge cases (empty, special chars, max length) covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for InMemorySessionTracker and OpenAiEmbeddingService</name>
  <files>
    tests/QdrantSkillsMCP.UnitTests/Session/InMemorySessionTrackerTests.cs,
    tests/QdrantSkillsMCP.UnitTests/Embedding/OpenAiEmbeddingServiceTests.cs
  </files>
  <behavior>
    InMemorySessionTracker tests:
    - MarkLoaded + IsLoaded: mark "skill-a" -> IsLoaded("skill-a") returns true
    - Not loaded: IsLoaded("unknown") returns false before any marking
    - GetLoadedSkills: mark "b", "a", "c" -> returns sorted ["a", "b", "c"]
    - Reset: mark skills, reset -> GetLoadedSkills returns empty, IsLoaded returns false
    - Duplicate mark: mark same skill twice -> no error, GetLoadedSkills has it once
    - Thread safety: concurrent MarkLoaded from multiple threads -> no exceptions, all skills tracked

    OpenAiEmbeddingService tests:
    - GenerateEmbeddingAsync: mock IEmbeddingGenerator to return known vector -> service returns same vector
    - Dimensions: returns value from QdrantSkillsOptions
    - Passes cancellation token through to generator
  </behavior>
  <action>
    For SessionTracker tests:
    - Create new `InMemorySessionTracker` instance per test (no shared state).
    - For thread safety test: use `Parallel.For` or `Task.WhenAll` with multiple concurrent MarkLoaded calls, assert no exceptions and all skills present.

    For EmbeddingService tests:
    - Use NSubstitute (or Moq, whichever was added in Plan 01) to mock `IEmbeddingGenerator<string, Embedding<float>>`.
    - Configure mock to return a known `Embedding<float>` with specific vector values.
    - Verify the service correctly extracts the float array.
    - Use `IOptions.Create(new QdrantSkillsOptions { VectorDimensions = 1536 })` for options.
  </action>
  <verify>
    <automated>cd C:/Development/MCP/QdrantSkillsMCP &amp;&amp; dotnet test tests/QdrantSkillsMCP.UnitTests -v normal</automated>
  </verify>
  <done>All unit tests pass. Session tracker is verified thread-safe. Embedding service correctly delegates to IEmbeddingGenerator. Full unit test suite green.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/QdrantSkillsMCP.UnitTests` passes with all tests green
- No tests depend on external services (Qdrant, OpenAI)
- Round-trip lossless test proves CRUD-05 at unit level
- Thread safety test for session tracker uses concurrent execution
</verification>

<success_criteria>
- All unit tests pass in < 30 seconds (no external dependencies)
- Parser round-trip test proves byte-for-byte fidelity
- Validator tests cover all rules from CONTEXT.md (lowercase, hyphens, numbers, max 64)
- Session tracker thread safety verified under concurrent load
- Embedding service delegation tested with mock
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-mcp-server/01-04-SUMMARY.md`
</output>
