---
phase: 01-core-mcp-server
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - tests/QdrantSkillsMCP.IntegrationTests/Fixtures/QdrantFixture.cs
  - tests/QdrantSkillsMCP.IntegrationTests/SkillCrudIntegrationTests.cs
  - tests/QdrantSkillsMCP.IntegrationTests/SkillSearchIntegrationTests.cs
  - tests/QdrantSkillsMCP.IntegrationTests/CollectionInitializerTests.cs
autonomous: true
requirements:
  - DIST-03

must_haves:
  truths:
    - "Integration tests run against a real Qdrant container managed by Aspire"
    - "Skills can be added, retrieved, updated, and deleted end-to-end"
    - "Semantic search returns results ranked by vector similarity"
    - "Collection is auto-created with correct dimensions on first use"
    - "Archived skills are excluded from search and list results"
  artifacts:
    - path: "tests/QdrantSkillsMCP.IntegrationTests/Fixtures/QdrantFixture.cs"
      provides: "Aspire test fixture with Qdrant container lifecycle"
      min_lines: 30
    - path: "tests/QdrantSkillsMCP.IntegrationTests/SkillCrudIntegrationTests.cs"
      provides: "End-to-end CRUD tests"
      min_lines: 80
    - path: "tests/QdrantSkillsMCP.IntegrationTests/SkillSearchIntegrationTests.cs"
      provides: "End-to-end search tests"
      min_lines: 60
    - path: "tests/QdrantSkillsMCP.IntegrationTests/CollectionInitializerTests.cs"
      provides: "Collection creation tests"
      min_lines: 30
  key_links:
    - from: "tests/QdrantSkillsMCP.IntegrationTests/Fixtures/QdrantFixture.cs"
      to: "Aspire AppHost"
      via: "DistributedApplicationTestingBuilder"
      pattern: "DistributedApplicationTestingBuilder"
    - from: "tests/QdrantSkillsMCP.IntegrationTests"
      to: "Qdrant container"
      via: "Aspire-managed container"
      pattern: "ResourceNotifications.*WaitForResourceHealthyAsync"
---

<objective>
Create integration tests that run the full stack against a real Qdrant container managed by Aspire. Tests verify CRUD operations, semantic search, collection auto-creation, and archive behavior end-to-end.

Purpose: Integration tests prove the system works as a whole -- Qdrant operations, embedding generation, and tool behavior against real infrastructure.
Output: Integration test suite that passes with `dotnet test tests/QdrantSkillsMCP.IntegrationTests`.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-mcp-server/01-CONTEXT.md
@.planning/phases/01-core-mcp-server/01-RESEARCH.md
@.planning/phases/01-core-mcp-server/01-02-SUMMARY.md
@.planning/phases/01-core-mcp-server/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Aspire test fixture and collection initializer integration tests</name>
  <files>
    tests/QdrantSkillsMCP.IntegrationTests/Fixtures/QdrantFixture.cs,
    tests/QdrantSkillsMCP.IntegrationTests/CollectionInitializerTests.cs
  </files>
  <action>
    1. **QdrantFixture.cs** -- Shared test fixture using Aspire testing:
       - Implements `IAsyncLifetime` (xunit.v3 async setup/teardown).
       - In `InitializeAsync`:
         - Create `DistributedApplicationTestingBuilder` from the AppHost project.
         - Build and start the distributed app.
         - Wait for Qdrant resource to be healthy: `app.ResourceNotifications.WaitForResourceHealthyAsync("qdrant", ct).WaitAsync(TimeSpan.FromSeconds(60), ct)`.
         - If health check is not available (Aspire #5768 issue), fall back to polling Qdrant's REST health endpoint with retry loop (max 30 seconds, 1-second intervals).
         - Create a `QdrantClient` from the connection string or known port.
         - Use a unique collection name per test run (e.g., `$"skills-test-{Guid.NewGuid():N}"`) to avoid cross-test pollution.
         - Expose: `QdrantClient`, `CollectionName`, `QdrantSkillsOptions` (with test collection name and dimensions).
       - In `DisposeAsync`:
         - Delete the test collection (cleanup).
         - Dispose the distributed app.
       - Use `[CollectionDefinition]` and `[Collection]` attributes to share the fixture across test classes (avoid starting multiple Qdrant containers).

    2. **CollectionInitializerTests.cs**:
       - Uses the QdrantFixture.
       - Test: `CollectionCreatedWithCorrectDimensions` -- create a CollectionInitializer with test options, call EnsureCollectionAsync, verify collection exists with correct vector size via `GetCollectionInfoAsync`.
       - Test: `CollectionCreationIsIdempotent` -- call EnsureCollectionAsync twice, no error on second call.
       - Test: `PayloadIndexesCreated` -- after EnsureCollectionAsync, verify payload indexes exist for `name`, `archived`, `updated_at` (check via collection info if Qdrant API exposes this, or just verify operations that use these indexes succeed).

    NOTE: Integration tests require Docker to be running for Aspire to provision the Qdrant container. Tests will be skipped/fail gracefully if Docker is not available.

    NOTE: For embedding in integration tests, use a MOCK embedding service (not real OpenAI) to avoid API key requirements in CI. Create a `FakeEmbeddingService` that returns deterministic vectors (e.g., hash-based vectors from input text) so search relevance can still be tested in a controlled way.
  </action>
  <verify>
    <automated>cd C:/Development/MCP/QdrantSkillsMCP &amp;&amp; dotnet build tests/QdrantSkillsMCP.IntegrationTests/QdrantSkillsMCP.IntegrationTests.csproj</automated>
  </verify>
  <done>Test fixture starts Qdrant via Aspire, waits for health, provides QdrantClient. Collection initializer tests verify auto-creation with correct dimensions and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Create CRUD and search integration tests</name>
  <files>
    tests/QdrantSkillsMCP.IntegrationTests/SkillCrudIntegrationTests.cs,
    tests/QdrantSkillsMCP.IntegrationTests/SkillSearchIntegrationTests.cs
  </files>
  <action>
    Both test classes use the shared QdrantFixture.

    1. **SkillCrudIntegrationTests.cs**:
       - `AddAndRetrieveSkill_RoundTripsLosslessly`: Add a skill with realistic markdown+YAML content, retrieve by name, assert `RawContent` is byte-for-byte identical to original.
       - `AddSkill_DuplicateName_ThrowsWithoutOverwrite`: Add skill "test-skill", add again without overwrite flag -> exception/error.
       - `AddSkill_DuplicateName_SucceedsWithOverwrite`: Add skill, add again with `overwrite=true` -> succeeds, content updated.
       - `UpdateSkill_ChangesContent`: Add skill, update with new content, retrieve -> new content returned, old content gone.
       - `DeleteSkill_RemovesPermanently`: Add skill, delete, retrieve -> null/not found.
       - `ArchiveSkill_ExcludesFromSearch`: Add skill, archive it, search for it -> not in results.
       - `ArchiveSkill_ExcludesFromList`: Add skill, archive it, list all -> not in list.
       - Use the `FakeEmbeddingService` from Task 1 for embedding generation.

    2. **SkillSearchIntegrationTests.cs**:
       - `SearchReturnsResultsRankedByScore`: Add 3 skills with different content, search with a query closer to one of them -> that skill has highest score.
       - `SearchRespectsMaxResults`: Add 5 skills, search with maxResults=2 -> only 2 returned.
       - `SearchRespectsScoreThreshold`: Add skills, search with high threshold -> fewer results than without threshold.
       - `ListSkillsReturnsAllNonArchived`: Add 3 skills, archive 1, list -> returns 2.
       - Use `FakeEmbeddingService` that generates deterministic but distinct vectors for different inputs (e.g., hash text to produce different vector patterns) so similarity ranking is testable.

    For the fake embedding service: create `FakeEmbeddingService : IEmbeddingService` that:
    - Takes text input, hashes it, and distributes hash bytes across a float[] of the configured dimension size.
    - Different inputs produce different vectors with measurable cosine similarity differences.
    - `Dimensions` returns a fixed value (e.g., 64 -- smaller than real embeddings for faster tests).
  </action>
  <verify>
    <automated>cd C:/Development/MCP/QdrantSkillsMCP &amp;&amp; dotnet test tests/QdrantSkillsMCP.IntegrationTests --no-build -v normal</automated>
  </verify>
  <done>All integration tests pass against real Qdrant. CRUD operations verified end-to-end. Search returns ranked results. Archive excludes from search/list. Round-trip lossless verified at integration level.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/QdrantSkillsMCP.IntegrationTests` passes (requires Docker for Qdrant container)
- `dotnet test` (full suite) passes -- both unit and integration tests green
- Round-trip test proves skill content is losslessly preserved through Qdrant storage
- Archive test proves archived skills are excluded from search and list
- Search ranking test proves results are ordered by similarity score
</verification>

<success_criteria>
- Aspire test fixture reliably starts Qdrant container and waits for health
- All CRUD operations (add, update, delete, archive) verified end-to-end
- Lossless round-trip verified at integration level (CRUD-05)
- Semantic search returns ranked results (SRCH-01)
- max-results and score threshold respected (SRCH-02, SRCH-03)
- Archived skills excluded from search and list (CRUD-04)
- Full test suite (`dotnet test`) passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-mcp-server/01-05-SUMMARY.md`
</output>
