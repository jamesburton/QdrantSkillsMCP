---
phase: 05-http-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj
autonomous: true
requirements: [TRANS-08]
must_haves:
  truths:
    - "dotnet pack succeeds with PackAsTool=true and FrameworkReference to Microsoft.AspNetCore.App"
    - "ModelContextProtocol upgraded to 1.2.0 and ModelContextProtocol.AspNetCore 1.2.0 added"
    - "dotnet build succeeds with no errors after package changes"
  artifacts:
    - path: "src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj"
      provides: "Updated package references and FrameworkReference"
      contains: "ModelContextProtocol.AspNetCore"
  key_links:
    - from: "QdrantSkillsMCP.Infrastructure.csproj"
      to: "Microsoft.AspNetCore.App"
      via: "FrameworkReference"
      pattern: "FrameworkReference Include=\"Microsoft.AspNetCore.App\""
---

<objective>
Upgrade MCP SDK packages and validate that PackAsTool + FrameworkReference to ASP.NET Core produces a valid NuGet tool package.

Purpose: This is the gating validation per D-10 -- if PackAsTool breaks with FrameworkReference, the entire HTTP transport strategy needs rethinking. Must pass before any HTTP code is written.
Output: Updated csproj with ModelContextProtocol 1.2.0, ModelContextProtocol.AspNetCore 1.2.0, and FrameworkReference to Microsoft.AspNetCore.App. Validated via dotnet pack + dotnet tool install round-trip.
</objective>

<execution_context>
@.claude/agents/workflows/execute-plan.md
@.claude/agents/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-http-transport/05-CONTEXT.md
@.planning/phases/05-http-transport/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade MCP SDK and add ASP.NET Core FrameworkReference</name>
  <files>src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj</files>
  <read_first>
    - src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj
    - .planning/phases/05-http-transport/05-RESEARCH.md (§Complete csproj Changes, §Pitfall 2)
  </read_first>
  <action>
    In `QdrantSkillsMCP.Infrastructure.csproj`:

    1. Upgrade existing ModelContextProtocol package from 1.1.0 to 1.2.0:
       Change: `<PackageReference Include="ModelContextProtocol" Version="1.1.0" />`
       To:     `<PackageReference Include="ModelContextProtocol" Version="1.2.0" />`

    2. Add ModelContextProtocol.AspNetCore 1.2.0 to the existing PackageReference ItemGroup:
       `<PackageReference Include="ModelContextProtocol.AspNetCore" Version="1.2.0" />`

    3. Add a NEW ItemGroup (separate from PackageReferences) with the FrameworkReference:
       ```xml
       <ItemGroup>
         <FrameworkReference Include="Microsoft.AspNetCore.App" />
       </ItemGroup>
       ```
       Place this new ItemGroup after the existing PackageReference ItemGroup.

    4. Do NOT use PackageReference for Microsoft.AspNetCore.App -- FrameworkReference resolves against the installed runtime, not bundled DLLs.

    After editing, run `dotnet build src/QdrantSkillsMCP.Infrastructure/ --no-restore` to verify compilation.
    Then run `dotnet build` (full solution) to verify no test project breakage.
  </action>
  <verify>
    <automated>dotnet build --verbosity quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - csproj contains `<PackageReference Include="ModelContextProtocol" Version="1.2.0" />`
    - csproj contains `<PackageReference Include="ModelContextProtocol.AspNetCore" Version="1.2.0" />`
    - csproj contains `<FrameworkReference Include="Microsoft.AspNetCore.App" />`
    - FrameworkReference is in its own ItemGroup, not mixed with PackageReferences
    - `dotnet build` exits 0 with no errors
  </acceptance_criteria>
  <done>Solution builds clean with upgraded MCP SDK 1.2.0 and ASP.NET Core FrameworkReference</done>
</task>

<task type="auto">
  <name>Task 2: Validate PackAsTool produces installable NuGet tool package</name>
  <files>src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj</files>
  <read_first>
    - src/QdrantSkillsMCP.Infrastructure/QdrantSkillsMCP.Infrastructure.csproj
    - .planning/phases/05-http-transport/05-RESEARCH.md (§Pitfall 2: PackAsTool + FrameworkReference)
  </read_first>
  <action>
    Validate the PackAsTool + FrameworkReference combination per D-10:

    1. Run `dotnet pack src/QdrantSkillsMCP.Infrastructure/ -c Release --output ./nupkg-test`
       - Must succeed without errors or warnings about FrameworkReference
       - Should produce a .nupkg file in ./nupkg-test/

    2. Uninstall any existing global tool: `dotnet tool uninstall -g QdrantSkillsMCP` (ignore errors if not installed)

    3. Install from local package: `dotnet tool install -g QdrantSkillsMCP --add-source ./nupkg-test --version "*-*"` (the --version "*-*" allows prerelease versions from MinVer)

    4. Verify the tool runs in stdio mode: `qdrant-skills-mcp --help` (or check it exits without crash)

    5. Clean up: `dotnet tool uninstall -g QdrantSkillsMCP` and `rm -rf ./nupkg-test`

    If `dotnet pack` fails:
    - Check if the error is related to FrameworkReference + PackAsTool interaction
    - Per D-11, explore minimal alternatives before splitting projects
    - Document the error in the summary for user decision

    Note: The project has `CreateRidSpecificToolPackages=false` which should help with framework-dependent tool packaging.
  </action>
  <verify>
    <automated>dotnet pack src/QdrantSkillsMCP.Infrastructure/ -c Release --output ./nupkg-test --verbosity quiet 2>&1 | tail -5 && ls ./nupkg-test/*.nupkg && rm -rf ./nupkg-test</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet pack` exits 0 with no errors
    - A .nupkg file is produced in the output directory
    - `dotnet tool install -g QdrantSkillsMCP --add-source ./nupkg-test` succeeds (tool installs without error)
    - The installed tool executes without TypeLoadException or missing framework errors
  </acceptance_criteria>
  <done>PackAsTool + FrameworkReference validated -- NuGet tool package installs and runs correctly with ASP.NET Core FrameworkReference included</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds (no compilation errors from package upgrade)
- `dotnet pack` produces a valid .nupkg
- Tool installs and runs from the packed NuGet package
- `dotnet test tests/QdrantSkillsMCP.UnitTests/ --verbosity quiet` still passes (no regression from package upgrade)
</verification>

<success_criteria>
PackAsTool + FrameworkReference is confirmed working. MCP SDK upgraded to 1.2.0. All existing tests pass. The project is ready for HTTP transport implementation in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-http-transport/05-01-SUMMARY.md`
</output>
