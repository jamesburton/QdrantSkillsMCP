---
phase: 05-http-transport
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - Dockerfile
autonomous: false
requirements: [TRANS-10, TRANS-09]
must_haves:
  truths:
    - "Dockerfile builds successfully with HTTP entrypoint"
    - "Container defaults to --http mode on port 8080"
    - "Container can be overridden to --stdio via CMD"
    - "Existing stdio mode works identically to v1.0 after all HTTP transport additions"
  artifacts:
    - path: "Dockerfile"
      provides: "Updated container image with HTTP entrypoint and EXPOSE 8080"
      contains: "ENTRYPOINT"
  key_links:
    - from: "Dockerfile"
      to: "Program.cs --http branch"
      via: "ENTRYPOINT dotnet QdrantSkillsMCP.Infrastructure.dll --http"
      pattern: "--http"
---

<objective>
Update Dockerfile for HTTP transport default and verify full stdio regression across the entire phase.

Purpose: Container deployments need HTTP mode as default (D-12). Final regression check ensures all HTTP additions haven't broken stdio transport (TRANS-09).
Output: Updated Dockerfile with EXPOSE 8080 and --http entrypoint. All tests green confirming no stdio regression.
</objective>

<execution_context>
@.claude/agents/workflows/execute-plan.md
@.claude/agents/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-http-transport/05-CONTEXT.md
@.planning/phases/05-http-transport/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfile for HTTP transport</name>
  <files>Dockerfile</files>
  <read_first>
    - Dockerfile
    - .planning/phases/05-http-transport/05-RESEARCH.md (§Dockerfile Update)
  </read_first>
  <action>
    Update the existing Dockerfile to support HTTP transport as default per D-12.

    **Changes to Stage 2 (runtime):**

    1. Add `EXPOSE 8080` after the `WORKDIR /app` line (before COPY):
       ```dockerfile
       EXPOSE 8080
       ```

    2. Replace the existing ENV block with an expanded version that includes URL config:
       ```dockerfile
       ENV ASPNETCORE_ENVIRONMENT=Production \
           QDRANT_SKILLS_URL=http://+:8080 \
           QdrantSkills__QdrantHost=qdrant \
           QdrantSkills__QdrantGrpcPort=6334 \
           QdrantSkills__CollectionName=skills \
           QdrantSkills__EmbeddingProvider=openai
       ```
       Note: `QDRANT_SKILLS_URL=http://+:8080` uses `+` wildcard to listen on all interfaces inside the container.

    3. Replace the existing ENTRYPOINT and remove the comment about stdio:
       Old:
       ```dockerfile
       # MCP server uses stdio transport — no ports needed
       ENTRYPOINT ["dotnet", "QdrantSkillsMCP.Infrastructure.dll"]
       ```
       New:
       ```dockerfile
       # Default to HTTP in container; override with: docker run <image> --stdio
       ENTRYPOINT ["dotnet", "QdrantSkillsMCP.Infrastructure.dll", "--http"]
       ```

    **Stage 1 (build) is unchanged** -- same SDK image, same restore/build/publish steps.

    The `aspnet:10.0` base image already includes the ASP.NET Core shared framework needed by the FrameworkReference.
  </action>
  <verify>
    <automated>grep -c "EXPOSE 8080" Dockerfile && grep -c "\-\-http" Dockerfile && grep -c "QDRANT_SKILLS_URL" Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile contains `EXPOSE 8080`
    - Dockerfile contains `QDRANT_SKILLS_URL=http://+:8080` in ENV block
    - Dockerfile ENTRYPOINT ends with `"--http"`
    - Dockerfile still uses `mcr.microsoft.com/dotnet/aspnet:10.0` as runtime base (already correct)
    - Dockerfile stage 1 (build) is unchanged
    - `grep "ENTRYPOINT" Dockerfile` shows `["dotnet", "QdrantSkillsMCP.Infrastructure.dll", "--http"]`
  </acceptance_criteria>
  <done>Dockerfile updated with EXPOSE 8080, QDRANT_SKILLS_URL env var, and --http entrypoint default</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify stdio regression and full test suite</name>
  <files>Dockerfile, src/QdrantSkillsMCP.Infrastructure/Program.cs</files>
  <action>
    Run the full test suite and manual smoke tests to verify the complete Phase 5 implementation.
    The executor runs `dotnet test --verbosity normal` and presents the results.
    Then presents the manual verification steps for the user to execute.
  </action>
  <verify>
    <automated>dotnet test --verbosity quiet 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. User has verified HTTP mode, stdio mode, conflict detection, and Dockerfile correctness.</done>
  <what-built>
    Complete HTTP transport implementation across Plans 01-03:
    - MCP SDK upgraded to 1.2.0 with ASP.NET Core FrameworkReference (Plan 01)
    - HTTP branch in Program.cs with Streamable HTTP + legacy SSE (Plan 02)
    - Health endpoints at /health and /health/json (Plan 02)
    - CORS, Kestrel tuning, configurable URL (Plan 02)
    - Transport flag parsing with conflict detection (Plan 02)
    - Unit tests for health check, flags, URL resolution (Plan 02)
    - Dockerfile with --http default (Plan 03)
  </what-built>
  <how-to-verify>
    1. Run full test suite: `dotnet test --verbosity normal`
       - All existing tests must pass (stdio regression safety)
       - New Health and Transport tests must pass

    2. Test HTTP mode manually (optional but recommended):
       ```
       dotnet run --project src/QdrantSkillsMCP.Infrastructure/ -- --http
       ```
       Then in another terminal:
       ```
       curl http://localhost:8080/health
       curl http://localhost:8080/health/json
       ```
       - /health should return 200
       - /health/json should return JSON with status, duration, checks

    3. Test stdio mode still works:
       ```
       echo '{"jsonrpc":"2.0","method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}},"id":1}' | dotnet run --project src/QdrantSkillsMCP.Infrastructure/ -- --stdio
       ```
       - Should return JSON-RPC response (may include Qdrant connection errors in stderr, that's fine)

    4. Test conflict detection:
       ```
       dotnet run --project src/QdrantSkillsMCP.Infrastructure/ -- --http --stdio
       ```
       - Should print error to stderr and exit with non-zero code

    5. Verify Dockerfile syntax (no Docker needed):
       - Review Dockerfile has EXPOSE 8080, --http entrypoint, QDRANT_SKILLS_URL env var
  </how-to-verify>
  <acceptance_criteria>
    - `dotnet test` exits 0 with all tests passing
    - HTTP mode starts and /health returns 200
    - stdio mode returns valid JSON-RPC response
    - --http --stdio prints error and exits non-zero
    - Dockerfile has EXPOSE 8080, QDRANT_SKILLS_URL, --http entrypoint
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `dotnet test` passes all tests (existing + new)
- Dockerfile has correct EXPOSE, ENV, and ENTRYPOINT
- Manual HTTP mode test shows health endpoints responding
- Manual stdio test shows MCP protocol still works
- Conflict detection works (--http --stdio exits with error)
</verification>

<success_criteria>
Phase 5 complete: Server supports HTTP mode (--http) with Streamable HTTP and legacy SSE, health endpoints, CORS, configurable URL, and Kestrel tuning. Stdio mode is unchanged. Dockerfile defaults to HTTP. PackAsTool produces valid NuGet package. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-http-transport/05-03-SUMMARY.md`
</output>
