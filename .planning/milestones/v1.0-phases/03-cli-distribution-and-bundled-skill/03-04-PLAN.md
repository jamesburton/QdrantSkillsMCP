---
phase: 03-cli-distribution-and-bundled-skill
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/QdrantSkillsMCP.Infrastructure/ServiceRegistration.cs
  - src/QdrantSkillsMCP.Infrastructure/Program.cs
  - tests/QdrantSkillsMCP.UnitTests/Setup/SetupWiringTests.cs
autonomous: true
gap_closure: true
requirements: [CLI-03, CLI-04, CLI-05, CLI-06, CLI-07]

must_haves:
  truths:
    - "Running `--setup` invokes SetupWizard.RunAsync and does NOT print 'not yet implemented'"
    - "SetupWizard, AgentDetector, and all IAgentConfigWriter implementations are resolvable from DI in setup mode"
    - "Setup mode does NOT register Qdrant infrastructure services (no Qdrant connection attempt)"
  artifacts:
    - path: "src/QdrantSkillsMCP.Infrastructure/ServiceRegistration.cs"
      provides: "AddSetupServices extension method registering all setup DI services"
      contains: "AddSetupServices"
    - path: "src/QdrantSkillsMCP.Infrastructure/Program.cs"
      provides: "Wired --setup branch calling SetupWizard.RunAsync"
      contains: "SetupWizard"
    - path: "tests/QdrantSkillsMCP.UnitTests/Setup/SetupWiringTests.cs"
      provides: "Tests proving DI registration resolves SetupWizard and all writers"
      min_lines: 20
  key_links:
    - from: "Program.cs"
      to: "SetupWizard.RunAsync"
      via: "DI resolution from host.Services"
      pattern: "GetRequiredService<SetupWizard>"
    - from: "ServiceRegistration.AddSetupServices"
      to: "IAgentConfigWriter implementations"
      via: "services.AddSingleton<IAgentConfigWriter, *Writer>"
      pattern: "AddSingleton<IAgentConfigWriter"
---

<objective>
Wire the --setup branch in Program.cs to the fully-implemented SetupWizard, closing the last verification gap in Phase 3.

Purpose: The SetupWizard, AgentDetector, and all 8+1 config writers are fully implemented and tested (51 unit tests pass), but Program.cs line 22-29 still prints a placeholder message. This plan registers all setup services in DI and replaces the placeholder with actual wizard invocation.

Output: Working `--setup` flag that resolves SetupWizard from DI and runs it, plus a DI wiring test.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-distribution-and-bundled-skill/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/QdrantSkillsMCP.Infrastructure/Setup/IAgentConfigWriter.cs:
```csharp
public interface IAgentConfigWriter
{
    string AgentName { get; }
    string WriterId { get; }
    bool CanAutoWrite { get; }
    AgentScope[] SupportedScopes { get; }
    string? DetectInstallation(AgentScope scope);
    string? SkillDirectoryPath { get; }
    Task WriteConfigAsync(string configPath, McpServerEntry entry);
    string GenerateSnippet(McpServerEntry entry, AgentScope scope);
}
```

From src/QdrantSkillsMCP.Infrastructure/Setup/SetupWizard.cs:
```csharp
public sealed class SetupWizard(
    AgentDetector detector,
    IEnumerable<IAgentConfigWriter> writers,
    ILogger<SetupWizard> logger)
{
    public async Task<int> RunAsync(string[] args);
}
```

From src/QdrantSkillsMCP.Infrastructure/Setup/AgentDetector.cs:
```csharp
public sealed class AgentDetector(IEnumerable<IAgentConfigWriter> writers)
{
    public IReadOnlyList<DetectedAgent> DetectInstalledAgents();
}
```

Config writer implementations in Setup/Writers/:
- ClaudeConfigWriter
- ClaudeDesktopConfigWriter
- CopilotConfigWriter
- CopilotCliConfigWriter
- CodexConfigWriter
- OpenCodeConfigWriter
- KiloCodeConfigWriter
- FactoryDroidConfigWriter
- SnippetFallbackWriter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register setup services in DI and wire Program.cs --setup branch</name>
  <files>src/QdrantSkillsMCP.Infrastructure/ServiceRegistration.cs, src/QdrantSkillsMCP.Infrastructure/Program.cs</files>
  <action>
1. In ServiceRegistration.cs, add a new public static extension method `AddSetupServices(this IServiceCollection services)` that registers:
   - All 9 IAgentConfigWriter implementations as singletons: ClaudeConfigWriter, ClaudeDesktopConfigWriter, CopilotConfigWriter, CopilotCliConfigWriter, CodexConfigWriter, OpenCodeConfigWriter, KiloCodeConfigWriter, FactoryDroidConfigWriter, SnippetFallbackWriter
   - AgentDetector as singleton
   - SetupWizard as singleton
   - Add `using QdrantSkillsMCP.Infrastructure.Setup;` and `using QdrantSkillsMCP.Infrastructure.Setup.Writers;` to the file
   - This method intentionally does NOT call AddQdrantSkillsInfrastructure (setup does not need Qdrant per Pitfall 4 in RESEARCH.md)

2. In Program.cs, replace lines 22-29 (the --setup placeholder) with:
   - Create a Host.CreateApplicationBuilder(args)
   - Call builder.Services.AddSetupServices()
   - Add logging services (builder already provides them)
   - Build the host
   - Resolve SetupWizard via host.Services.GetRequiredService&lt;SetupWizard&gt;()
   - Call await wizard.RunAsync(args)
   - Exit with the returned exit code
   - Remove the placeholder comment and Console.WriteLine
  </action>
  <verify>
    <automated>cd "C:/Development/MCP/QdrantSkillsMCP" && dotnet build src/QdrantSkillsMCP.Infrastructure --no-restore -v q</automated>
  </verify>
  <done>Program.cs --setup branch resolves SetupWizard from DI and calls RunAsync. No placeholder text remains. Build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DI wiring integration test for setup services</name>
  <files>tests/QdrantSkillsMCP.UnitTests/Setup/SetupWiringTests.cs</files>
  <behavior>
    - Test 1: AddSetupServices registers SetupWizard resolvable from ServiceProvider
    - Test 2: AddSetupServices registers AgentDetector resolvable from ServiceProvider
    - Test 3: AddSetupServices registers all 9 IAgentConfigWriter implementations (resolve IEnumerable&lt;IAgentConfigWriter&gt; and assert count >= 9)
    - Test 4: AddSetupServices does NOT register ISkillRepository (proving Qdrant services are absent)
  </behavior>
  <action>
Create tests/QdrantSkillsMCP.UnitTests/Setup/SetupWiringTests.cs with xunit.v3 tests that:
- Build a ServiceCollection, call AddSetupServices() and AddLogging(), build the provider
- Verify SetupWizard, AgentDetector, and IEnumerable&lt;IAgentConfigWriter&gt; all resolve
- Verify ISkillRepository does NOT resolve (throws InvalidOperationException)
- Use [Fact] attributes, follow existing test conventions in the project
  </action>
  <verify>
    <automated>cd "C:/Development/MCP/QdrantSkillsMCP" && dotnet test tests/QdrantSkillsMCP.UnitTests --filter "FullyQualifiedName~SetupWiring" --no-restore -x</automated>
  </verify>
  <done>All 4 DI wiring tests pass. SetupWizard is provably resolvable. Qdrant services provably absent in setup mode.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with no errors
2. `dotnet test tests/QdrantSkillsMCP.UnitTests --no-restore -x` -- all existing 173+ tests still pass (no regressions)
3. `dotnet test tests/QdrantSkillsMCP.UnitTests --filter "FullyQualifiedName~SetupWiring" -x` -- new wiring tests pass
4. `grep -c "not yet implemented" src/QdrantSkillsMCP.Infrastructure/Program.cs` returns 0 (placeholder removed)
</verification>

<success_criteria>
- The --setup branch in Program.cs calls SetupWizard.RunAsync (not a placeholder)
- All setup services (SetupWizard, AgentDetector, 9 config writers) are registered in DI via AddSetupServices
- Setup mode does NOT register Qdrant infrastructure (no ISkillRepository, no QdrantClient, no IHostedService)
- All existing tests pass (zero regressions)
- New DI wiring tests prove the registration works
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-distribution-and-bundled-skill/03-04-SUMMARY.md`
</output>
